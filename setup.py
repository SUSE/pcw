from setuptools import setup, find_packages

setup(
        install_requires=[
            'boto3',
            'azure',
            'django',
            'django-tables2',
            'django-filter',
            'django-bootstrap3'
            ],
)
